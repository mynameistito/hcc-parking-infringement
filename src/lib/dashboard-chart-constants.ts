/** Ranked streets returned for dashboard bar charts (scrollable). */
export const DASHBOARD_CHART_STREET_LIMIT = 100;

/** Explore workbench list depth (compact tables). */
export const DASHBOARD_EXPLORE_RANK_LIMIT = 25;
