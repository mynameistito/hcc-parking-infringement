/** Recent infringements included in the live WebSocket dashboard snapshot. */
export const RECENT_INFRINGEMENTS_LIMIT = 100;

/** Default page size for infringement list requests. */
export const INFRINGEMENTS_DEFAULT_PAGE_SIZE = 100;

/** Maximum allowed `limit` on `/api/v1/infringements/recent`. */
export const INFRINGEMENTS_MAX_PAGE_SIZE = 1000;
